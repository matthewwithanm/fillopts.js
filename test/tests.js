/*jshint strict:false */
/*globals describe, it */

var
    assert = require('chai').assert,
    fillopts = require('../index');


describe('fillopts', function () {
    it('passes a test', function () {
        assert(true);
    });
});
